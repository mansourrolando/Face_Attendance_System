# تحديثات المراقبة الأمنية - الإصدار v3
**التاريخ:** يونيو 2026
**الهدف:** تعزيز قدرات المراقبة الأمنية في نظام الحضور الذكي

---

## ملخص التعديلات

تم إضافة 3 حقول جديدة لجدول `audit_log` لتسجيل معلومات أمنية شاملة عن كل إجراء في النظام، خصوصاً المحاولات الفاشلة لتسجيل الدخول.

---

## التعديلات المنجزة (6 ملفات)

### 1. `instance/models.py` — AuditLog Model
**أضيفت 3 أعمدة جديدة:**
- `ip_address` (VARCHAR 45) — يدعم IPv4 و IPv6
- `user_agent` (VARCHAR 255) — معلومات المتصفح/الجهاز
- `success` (BOOLEAN, default True) — حالة الإجراء (ناجح/فاشل)

### 2. `utils/helpers.py` — log_action() function
**تم توسيع الدالة لقبول معاملات جديدة:**
- `success=True/False` — حالة الإجراء
- `ip_address` — يُجلب تلقائياً من `request.remote_addr` إذا لم يُمرر
- `user_agent` — يُجلب تلقائياً من `request.headers.get('User-Agent')`
- `user_id` / `user_name` — لتسجيل محاولات بأسماء مستخدمين لم يتم التحقق منهم (مثل `login_failed`)

**الميزة:** الـbackward compatibility — كل النداءات القديمة (مثل `log_action('logout', ...)` ما زالت تعمل دون تغيير.

### 3. `routes/auth.py` — تسجيل المحاولات الفاشلة
**أضيف سجل للمحاولات الفاشلة بقاعدة البيانات** (إضافة لـRate Limiting الموجود في الذاكرة):

| الحالة | الإجراء المُسجّل | الوصف |
|--------|----------------|-------|
| مستخدم موجود + كلمة مرور خاطئة | `log_action('login_failed', success=False, user_id=user.id)` | يحمل `user_id` و `user_name` الفعليين |
| مستخدم غير موجود أصلاً | `log_action('login_failed', success=False, user_id=None, user_name=username)` | يحمل اسم المستخدم المُحاول (لتتبع هجمات تخمين الأسماء) |

### 4. `app.py` — ترحيل تلقائي (Migration)
**أضيف كود ترحيل آمن** يضيف الأعمدة الجديدة لقواعد البيانات القديمة:
- يفحص وجود العمود قبل إضافته
- يتجاهل أخطاء "duplicate column" بهدوء
- يضيف فهرس مركّب `idx_audit_ip_success` لتسريع البحث عن المحاولات الفاشلة من IP معيّن
- **لا يفقد أي بيانات قديمة** (السجلات القديمة تأخذ `success=True` كقيمة افتراضية)

### 5. `routes/audit.py` — فلاتر وأكثر
**أضيفت فلاتر جديدة في صفحة سجل النشاط:**
- فلتر بالحالة (`success` / `failed`)
- فلتر بعنوان IP (بحث جزئي)
- إحصائيات أمنية سريعة: عدد المحاولات الفاشلة + عدد عناوين IP المختلفة

### 6. `templates/audit_log.html` — واجهة العرض
**أضيفت أعمدة جديدة:**
- عمود "الحالة" مع أيقونة ✓ (ناجح) أو ✗ (فاشل)
- عمود "IP" بخط monospace
- عمود "المتصفح" (User-Agent) قابل للتوسيع عند الـhover
- شارة حمراء `badge-login_failed` للإجراءات الفاشلة
- صفوف ملونة: المحاولات الفاشلة بخلفية حمراء فاتحة
- بطاقتان للإحصائيات الأمنية أعلى الصفحة

---

## كيفية الاستخدام

### تشغيل المشروع بعد التحديث:
```bash
# 1. نفس خطوات التشغيل العادية - لا حاجة لأي خطوة إضافية
python app.py

# 2. عند أول تشغيل، سيقوم النظام تلقائياً بـ:
#    - إضافة الأعمدة الجديدة لـaudit_log (إذا لم تكن موجودة)
#    - إنشاء الفهرس الجديد
#    - لن يتم فقدان أي سجل قديم
```

### الاختبار اليدوي:
1. سجّل الدخول بكلمة مرور خاطئة 3 مرات
2. افتح صفحة **سجل النشاط** (`/audit_log`)
3. يجب أن ترى:
   - 3 سجلات بإجراء "فاشل" باللون الأحمر
   - عمود IP يعرض عنوانك (127.0.0.1 محلياً)
   - عمود المتصفح يعرض معلومات متصفحك
4. استخدم فلتر "الحالة: فاشل" لرؤية المحاولات الفاشلة فقط

---

## الفوائد الأمنية

| قبل التحديث | بعد التحديث |
|------------|------------|
| المحاولات الفاشلة تُخزّن بالـRAM فقط (تُفقد عند إعادة التشغيل) | تُخزّن في قاعدة البيانات بشكل دائم |
| لا يمكن معرفة من حاول الدخول (IP مجهول) | يُسجَّل IP + User-Agent لكل محاولة |
| لا يمكن تمييز محاولة على مستخدم موجود vs. تخمين أسماء | يتم تمييزهما بـ`user_id` (NULL لغير الموجودين) |
| لا توجد إحصائيات أمنية | بطاقات إحصائية + فلاتر متقدمة |
| Rate Limiting فقط (لا تاريخ للهجمات) | تاريخ كامل + Rate Limiting |

---

## التوافق مع الأطروحة

يمكنك الآن أن تكتب في فصل الأمان بالأطروحة:

> "يقوم النظام بتسجيل جميع محاولات الوصول (الناجحة والفاشلة) في قاعدة البيانات مع المعلومات التالية: اسم المستخدم، عنوان IP، نوع المتصفح (User-Agent)، الطابع الزمني، وحالة الإجراء (ناجح/فاشل). كما يميّز النظام بين محاولات الدخول الفاشلة على مستخدمين موجودين فعلاً (احتمال كلمة مرور منسية) ومحاولات على مستخدمين غير موجودين (احتمال هجوم تخمين أسماء المستخدمين). تُحفظ هذه السجلات بشكل دائم لتحليلها لاحقاً، بالتوازي مع آلية Rate Limiting الفورية لمنع هجمات القوة الغاشمة."
