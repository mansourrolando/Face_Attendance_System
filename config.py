import os

# ==========================================
# إعدادات Flask الأساسية
# ==========================================

SECRET_KEY = os.environ.get('SECRET_KEY', os.urandom(24).hex())

# قاعدة البيانات
basedir = os.path.abspath(os.path.dirname(__file__))
SQLALCHEMY_DATABASE_URI = 'sqlite:///' + os.path.join(basedir, 'instance', 'attendance_system.db')
SQLALCHEMY_TRACK_MODIFICATIONS = False

# ==========================================
# إعدادات التعرف على الوجه
# ==========================================

# الحد الأدنى لحجم الوجه (بالبكسل)
FACE_MIN_SIZE = 60

# عتبة وضوح الصورة (Laplacian Variance)
FACE_BLUR_THRESHOLD = 30

# الحد الأقصى لعدد الوجوه المسموح بها في الصورة
FACE_MAX_FACES_ALLOWED = 1

# تفعيل تطبيع L2 للمتجهات
FACE_NORMALIZE_EMBEDDINGS = True

# عدد الصور المطلوبة لتسجيل بصمة الوجه
FACE_REGISTRATION_IMAGES = 5

# الحد الأدنى للتشابه بين صور التسجيل
FACE_REGISTRATION_MIN_SIMILARITY = 0.5

# عتبة التعرف على الوجه (Cosine Similarity)
# V7 from-scratch CNN: Best threshold = 0.47 on LFW, لكن 0.55 أفضل لنظام الحضور (أمان أكبر)
FACE_RECOGNITION_THRESHOLD = 0.55

# الحد الأدنى للفجوة بين أفضل تطابق وثاني أفضل تطابق
FACE_GAP_THRESHOLD = 0.08

# ==========================================
# إعدادات كشف الشاشات (Screen Detection) - الإصدار 2.0
# يمنع الاحتيال عن طريق عرض صورة الوجه على شاشة هاتف/تابلت
#
# v2 تستخدم 5 تقنيات:
# 1. كشف نمط Moiré متعدد المقاييس بـ FFT (30%)
# 2. كشف الدورية بالارتباط التلقائي (20%)
# 3. تحليل الملمس/التباين (20%)
# 4. تحليل لوني (15%)
# 5. كشف الانعكاسات (15%)
# ==========================================

# تفعيل/تعطيل كشف الشاشات
SCREEN_DETECTION_ENABLED = True

# عتبة القرار النهائية لكشف الشاشة (0.0 - 1.0)
# إذا كانت الدرجة النهائية >= هذه القيمة = شاشة مكتشفة
# 0.30 = حساس جداً (قد يعطي إيجابيات كاذبة على وجوه حقيقية)
# 0.40 = متوازن (يحتاج أكثر من مؤشر واحد للتأكيد)
# 0.50 = صارم (يقلل الإيجابيات الكاذبة بشكل كبير)
SCREEN_DECISION_THRESHOLD = 0.40

# عتبة تباين الملمس (Laplacian Variance)
# إذا كان التباين أقل من هذه القيمة = ملمس مسطح = مشبوه (شاشة)
SCREEN_TEXTURE_THRESHOLD = 60.0

# الحد الأدنى لحجم الوجه لتحليل الشاشة (بالبكسل)
SCREEN_MIN_FACE_SIZE = 40

# إعدادات إضافية (محفوظة للتوافق)
SCREEN_FFT_THRESHOLD = 0.10
SCREEN_EDGE_THRESHOLD = 0.08
SCREEN_COLOR_SCORE_WEIGHT = 0.15

# ==========================================
# إعدادات الدوام الافتراضية
# ==========================================

WORK_START_TIME = '08:00'
WORK_END_TIME = '17:00'
GRACE_PERIOD_MINUTES = 15

# أقرب وقت مسموح لتسجيل الحضور (لا يمكن تسجيل الحضور قبل هذا الوقت)
ATTENDANCE_EARLIEST_TIME = '07:00'

# ==========================================
# إعدادات أيام العطلة الأسبوعية
# ==========================================

# أيام العطلة الأسبوعية (0=الاثنين, 1=الثلاثاء, ..., 4=الجمعة, 5=السبت, 6=الأحد)
WEEKEND_DAYS = '4,5'

# ==========================================
# إعدادات رصيد الإجازات الافتراضي
# ==========================================

# رصيد الإجازة السنوية الافتراضي (بالأيام)
DEFAULT_ANNUAL_LEAVE_BALANCE = 14

# رصيد الإجازة المرضية الافتراضي (بالأيام)
DEFAULT_SICK_LEAVE_BALANCE = 10

# ==========================================
# إعدادات الانصراف التلقائي
# ==========================================

# تفعيل الانصراف التلقائي لمن نسي تسجيل الانصراف
AUTO_CHECKOUT_ENABLED = True

# وقت الانصراف التلقائي (بعد كم ساعة من نهاية الدوام)
AUTO_CHECKOUT_AFTER_HOURS = 1

# ==========================================
# إعدادات تقييد تسجيل الدخول
# ==========================================

# الحد الأقصى لمحاولات تسجيل الدخول الفاشلة
MAX_LOGIN_ATTEMPTS = 5

# مدة الحظر بعد تجاوز المحاولات (بالدقائق)
LOGIN_LOCKOUT_MINUTES = 5

# ==========================================
# إعدادات الكشك (Kiosk Mode)
# ==========================================

# رمز PIN لحماية وضع الكشك (لتعطيل الكشك أو الدخول للإعدادات)
KIOSK_PIN = '1234'

# مدة عرض شاشة الترحيب بعد التعرف على الوجه (بالثواني)
KIOSK_WELCOME_DURATION = 5
